---
name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  lintro:
    permissions:
      contents: read
      packages: read
    uses: lgtm-hq/lgtm-ci/.github/workflows/reusable-quality.yml@main
