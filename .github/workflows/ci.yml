---
name: CI

"on":
  pull_request:
    branches: [main]
  push:
    branches: [main]
  merge_group:

jobs:
  quality:
    permissions:
      contents: read
      packages: read  # pull lintro-image from GHCR in reusable-quality-lint
    # yamllint disable-line rule:line-length
    uses: lgtm-hq/lgtm-ci/.github/workflows/reusable-quality-lint.yml@a62235f19f6eccade11bf4840967c3d5daa9c9b1  # v0.68.1
    with:
      tooling-ref: "a62235f19f6eccade11bf4840967c3d5daa9c9b1"  # v0.68.1
      job-name: "🛠️ Lintro Code Quality"
