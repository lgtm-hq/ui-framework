---
# ui-framework lintro configuration.
# JS/TS sources are gated by Biome and the frontend toolchain; lintro covers
# the cross-language checks. No first-party Python lives here, so pylint
# stays enabled (per lint policy) and no-ops on this repository.
